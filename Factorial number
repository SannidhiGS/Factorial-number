<?php
function factorial($n) {
    if ($n <= 1) {
        return 1;
    } else {
        return $n * factorial($n - 1);
    }
}

$number = 5; // You can change this number to calculate the factorial of a different number
$result = factorial($number);
echo "The factorial of $number is $result.";
?>
